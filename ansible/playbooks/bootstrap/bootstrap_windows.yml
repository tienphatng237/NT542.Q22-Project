---
- import_playbook: ../pipeline/resolve_windows_runtime.yml

- name: Phase 0 - Windows Management Bootstrap
  hosts: windows_runtime
  gather_facts: false
  tasks:
    - ansible.builtin.include_role:
        name: windows_management_bootstrap
